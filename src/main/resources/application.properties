# ========== SERVER ==========
spring.application.name=soccorsoweb-services
server.port=${PORT:8080}

# ========== DATABASE - Railway MySQL ==========
spring.datasource.url=jdbc:mysql://${MYSQLHOST:localhost}:${MYSQLPORT:3306}/${MYSQLDATABASE:railway}
spring.datasource.username=${MYSQLUSER:root}
spring.datasource.password=${MYSQLPASSWORD:password}
spring.datasource.driver-class-name=com.mysql.cj.jdbc.Driver

# ========== JPA / HIBERNATE ==========
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=false
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.MySQLDialect

# ========== JWT SECURITY ==========
jwt.secret=${JWT_SECRET:changeme}
jwt.expiration=86400000

# ========== SWAGGER UI ==========
springdoc.api-docs.path=/api-docs
springdoc.swagger-ui.path=/swagger-ui.html
springdoc.api-docs.enabled=true

# ========== EMAIL - RESEND API ==========
resend.api.key=${RESEND_API_KEY}
resend.from.email=${RESEND_FROM_EMAIL:noreply@soccorsoweb.it}

# ========== MAIL SMTP (DISABILITATO - Non usato) ==========
# spring.mail.host=${MAIL_HOST:localhost}
# spring.mail.port=${MAIL_PORT:1025}
# spring.mail.username=${MAIL_USERNAME:}
# spring.mail.password=${MAIL_PASSWORD:}
# spring.mail.properties.mail.smtp.auth=${MAIL_SMTP_AUTH:false}
# spring.mail.properties.mail.smtp.starttls.enable=${MAIL_SMTP_STARTTLS:false}

# ========== APP URLs ==========
app.base-url=${APP_BASE_URL:https://soccorsoweb-swa-production.up.railway.app}
app.frontend-url=${APP_FRONTEND_URL:http://localhost:5173}

# ========== LOGGING ==========
logging.level.root=INFO
logging.level.org.springdoc=INFO
logging.level.org.springframework.web=INFO
logging.level.it.univaq.swa.soccorsoweb=DEBUG
